THISDIR := $(realpath .)

#EXECUTABLE = predict
LIBRARY = libpredict.so

CPPFLAGS  =
INCLUDES  = \
 -I /usr/local/include/opencv2 

LDFLAGS   = \
 -L /usr/local/lib \
 -lopencv_core \
 -lopencv_highgui

include ../Makefile.lib

